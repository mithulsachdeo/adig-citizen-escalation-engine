# Server-side ONLY. Read by the /api/narrative serverless route to call Google Gemini.
# Never exposed to the browser and never committed (.env / .env*.local are gitignored).
# Get a key from Google AI Studio (https://aistudio.google.com/apikey).
GEMINI_API_KEY=
